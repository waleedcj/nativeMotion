---
title: Install React Native Reanimated
description: React Native Reanimated comes pre-installed with the latest Expo SDK. For older versions or custom projects, follow the installation guide below.
tags: [expo, react-native, reanimated]
keywords:
  [
    "install react native reanimated",
    "react native reanimated installation",
    "expo reanimated setup",
    "how to install reanimated react native",
    "react native reanimated guide",
    "reanimated setup expo",
    "configure babel react native reanimated",
    "react native reanimated plugin",
    "clear metro bundler cache",
    "react native animations setup",
    "react native reanimated troubleshooting",
    "reanimated library install",
    "npx expo install reanimated",
    "yarn add react native reanimated",
    "react native custom babel config",
    "react native performance animations setup",
    "expo sdk reanimated",
    "react native development tools",
    "react native animation library setup",
    "react native environment setup",
  ]
author: "walid memon"
ogImage: "/searchBar.webp"
lastModified: "2025-05-15T10:00:00Z"
---

<Tabs defaultValue="npm" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="npm">npm</TabsTrigger>
    <TabsTrigger value="yarn">yarn</TabsTrigger>
  </TabsList>
  <TabsContent value="npm">
  <Stepper>
  <StepperItem title="Step 1: Install Reanimated">
    ```plaintext
    npx expo install react-native-reanimated
    ```
  </StepperItem>

  <StepperItem title="Step 2: Configure babel.config.js (if not using default Expo config)">
    Only add this if you're customizing the babel configuration:

    ```javascript
    module.exports = {
        presets: [
        ... // don't add it here :)
        ],
        plugins: [
        ...
        'react-native-reanimated/plugin',
        ],
  };
    ```
  </StepperItem>

  <StepperItem title="Step 3: Clear Metro bundler cache">
    ```plaintext
  npx expo start -c
    ```
  </StepperItem>
</Stepper>

  </TabsContent>
  <TabsContent value="yarn">
 <Stepper>
  <StepperItem title="Step 1: Install Reanimated">
    ```plaintext
   yarn add react-native-reanimated
    ```
  </StepperItem>

  <StepperItem title="Step 2: Configure babel.config.js (if not using default Expo config)">
    Only add this if you're customizing the babel configuration:

    ```javascript
    module.exports = {
        presets: [
        ... // don't add it here :)
        ],
        plugins: [
        ...
        'react-native-reanimated/plugin',
        ],
  };
    ```
  </StepperItem>

  <StepperItem title="Step 3: Clear Metro bundler cache">
    ```plaintext
  yarn expo start -c
    ```
  </StepperItem>
</Stepper>
  </TabsContent>
</Tabs>
